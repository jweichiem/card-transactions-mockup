# @jweichiem-mockup/cards

Placeholder workspace for a future cards-focused microfrontend.

## Intent

- Reserve an isolated app package for cards-specific UI/routes.
- Keep boundaries aligned with the `apps/` workspace conventions.

## Current Structure

- `src/index.ts`: placeholder export only.
- `tsconfig.json`: TypeScript configuration.
- `package.json`: local scripts and workspace metadata.

## Current Status

- No runtime app implementation yet.
- `dev` script currently prints a placeholder message.

## Scripts

- `yarn workspace @jweichiem-mockup/cards dev`
- `yarn workspace @jweichiem-mockup/cards build`
- `yarn workspace @jweichiem-mockup/cards lint`
- `yarn workspace @jweichiem-mockup/cards test`
